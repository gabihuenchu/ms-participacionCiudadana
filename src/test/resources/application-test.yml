spring:
  datasource:
    url: jdbc:tc:postgresql:15:///catastrofescl_citizen
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  rabbitmq:
    listener:
      simple:
        auto-startup: false

firebase:
  enabled: false
