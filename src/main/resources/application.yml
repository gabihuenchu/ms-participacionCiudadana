server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: ms-citizen

  datasource:
    # BD compartida del monorepo: catastrofescl (misma que ms-resources, fuente de verdad del inventario)
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/catastrofescl}
    username: ${DATABASE_USERNAME:catastrofescl}
    password: ${DATABASE_PASSWORD:catastrofescl}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    table: flyway_schema_history_ms_citizen
    baseline-on-migrate: true
    baseline-version: "0"

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        default-requeue-rejected: false
        acknowledge-mode: manual

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

firebase:
  enabled: ${FIREBASE_ENABLED:false}
  project-id: ${FIREBASE_PROJECT_ID:}
  credentials-path: ${FIREBASE_CREDENTIALS_PATH:}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    cl.catastrofescl.citizen: INFO
